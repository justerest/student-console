{
  "name": "student-console",
  "version": "0.5.11",
  "description": "template for js console program",
  "main": "index.ts",
  "directories": {
    "lib": "lib",
    "src": "src"
  },
  "scripts": {
    "ts": "tsc -p ./",
    "dev": "npm run ts && node index.js -dev",
    "test": "npm run ts && node index.js",
    "build": "npm run ts && pkg index.js -t win -o ./dist/program",
    "build-linux": "npm run ts && pkg index.js -o ./dist/program",
    "doc": "npm run ts && documentation readme -s lib index.js"
  },
  "keywords": [
    "console",
    "template",
    "pascal"
  ],
  "author": "justerest",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^8.5.2",
    "documentation": "^5.3.5",
    "pkg": "^4.3.0-beta.5",
    "typescript": "^2.6.2"
  }
}
